id: illegal-enum
base: examples/rca/inconclusive.json
schema: schemas/rca.schema.json
mutations:
  - op: set
    path: /severity
    value: SEVERE
expectedReasons:
  - "is not one of ['LOW', 'MEDIUM', 'HIGH', 'CRITICAL']"
