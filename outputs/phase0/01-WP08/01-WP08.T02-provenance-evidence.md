# 01-WP08.T02 单 Source 与 provenance 证据

状态：`PASS`

共享合同逐一断言每次调用只返回该 Adapter 的一个 SourceDescriptor；Query 保存模板、参数 SHA-256、时间窗和 Resource。原始响应以不可变 RawArtifact 保存 UUID、媒体类型、内容和 SHA-256，每个 ObservationRecord 引用同一个 Artifact。

追溯测试从每条 Evidence 的 `RUNTIME_OBSERVATION` provenance 依次找到 Batch、ObservationRecord、Source、Resource 和 Artifact，并重新计算 Artifact 内容哈希相等。
