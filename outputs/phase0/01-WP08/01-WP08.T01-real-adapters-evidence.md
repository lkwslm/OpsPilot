# 01-WP08.T01 五类真实 Adapter 证据

状态：`PASS`

实现并调用了 `PrometheusMetricAdapter`、`JaegerTraceAdapter`、`JsonlLogAdapter`、`SpringActuatorHealthAdapter` 和 `StaticComposeTopologyAdapter`。Prometheus/Jaeger 通过本地真实 HTTP 回放端点读取响应，JSONL/Actuator/Compose 从真实文件读取；15 个 fixture 分别保存成功、合法空结果和 Schema 错误输入。

每个 Adapter 的 `sourceId/sourceKind/adapterId/adapterVersion/connectionRef/scope/capability` descriptor 固定，五个 sourceId 唯一。测试结果为 4 tests、0 failure、0 error。
