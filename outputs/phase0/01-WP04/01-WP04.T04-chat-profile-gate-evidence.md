# 01-WP04.T04 Chat Profile 汇总门禁证据

状态：`PASS`

门禁已逐一回填 `default` 与六角色逻辑 Profile。每个 Profile 的配置模型和真实 Provider 返回模型均为 `deepseek-v4-flash`，必需能力、上下文、Secret ref 和配额没有缺口。

独立 fail-fast 测试覆盖：

- 模型 ID 为空：`MODEL_EMPTY`；
- Key 缺失：`SECRET_REF_UNRESOLVED`；
- 上下文上限为 0/未知：`CONTEXT_WINDOW_UNKNOWN`；
- 必需能力真实探针未通过：`CAPABILITY_UNSUPPORTED:<capability>`；
- 配额不足：`QUOTA_INSUFFICIENT`；
- 任一逻辑 Profile 未回填：`PROFILE_UNVERIFIED`。

以上任一缺口都会令 CLI 非零退出。当前真实汇总为 `PASS`，七个 Profile 的 `gaps` 均为空。模型工具套件共 13 个测试全部通过。

## SHA-256

- `gate_chat_profiles.py`: `96d1c26ecea6b01029daa95e9b81efcba6ec00efe2a5eb2b88f9ca242de330cc`
- `test_gate_chat_profiles.py`: `0a7590bce9e83888798946c98c3cd84a5940dde6ffb3dfb55150e3245332f23b`
