# 01-WP04.T01 Phase 0 Profile 解析证据

状态：`PASS`

默认配置和六角色稀疏覆盖已固化在 `deployment/model-profiles.phase0.yaml`。解析顺序为默认值后应用角色嵌套覆盖，输出 `default` 加六个角色共七个逻辑 Profile，并按 Provider、协议、Base URL、模型 ID 去重为一个实际模型。

固定 Chat 模型为 `deepseek-v4-flash`，Provider 为 `deepseek`，上下文上限 `1,000,000` tokens。选择依据是 2026-07-18 的 DeepSeek 官方模型文档；旧 `deepseek-chat` 将在 2026-07-24 停用，因此没有采用即将失效的别名。

配置只保存 `env:DEEPSEEK_API_KEY` 引用。解析器拒绝 `apiKey`、`secretValue` 等内嵌密钥字段，也不读取环境变量值。测试注入标记密钥并确认输出中不存在该值。

测试结果：4 个测试全部通过，覆盖七 Profile 完整展开、嵌套覆盖优先级、默认字段保留、密钥不读取/不输出和内嵌密钥拒绝。

## 输入文件 SHA-256

- `deployment/model-profiles.phase0.yaml`: `0ca689f8adcfcd9159a371e09bba561811988da90073980bc71d2a52af5f037f`
- `scripts/models/resolve_profiles.py`: `693183547dae465f9c2794c4f18c621a5baa8394392e8452070a8bc9ca2d1d15`
- `scripts/models/test_resolve_profiles.py`: `36820bd3bcbd422359b7896bcb8c4c932d1a912c686a42cb2dd849d1e5c8dd4a`
