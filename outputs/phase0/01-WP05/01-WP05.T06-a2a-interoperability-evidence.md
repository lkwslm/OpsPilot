# 01-WP05.T06 A2A 24.6 互操作汇总证据

状态：`PASS`

服务端运行在独立子 JVM，测试断言其 PID 与客户端所在父 JVM 不同。版本化审计日志捕获真实 `POST /a2a/messages:send` 与 `GET /a2a/tasks/{id}`，POST 的 Content-Type 为 `application/json`；日志只含 PID、方法、路径和 Content-Type，不含正文、凭证或模型密钥。

子进程关闭后，客户端继续调用原 URL 得到 `A2A_NETWORK_FAILURE`，不存在进程内回退。ArchUnit 同时禁止 client 包依赖 server 包或任何 Spring 类型，从结构和运行两层排除 Spring Bean 直调。

WP05 全量回归共 9 个测试通过，0 failure，0 error，`BUILD SUCCESS`。证据目录密钥模式扫描为 `PASS`。

## 第 24.6 节断言汇总

- A2A Card：六角色、协议 `v1.0.1` / wire `1.0`、HTTP+JSON；
- 操作：send、stream、get、cancel、subscribe；
- 持久与恢复：PostgreSQL Task/Event、断流重放、Server/Client 重启；
- 幂等：`messageId + request hash`、并发无重复副作用、hash 冲突 HTTP 409；
- 安全：Artifact 六项校验、专业 Agent 数据库角色无领域写权限；
- 生命周期：取消/完成竞争、终态单调；
- 传输：跨 JVM HTTP 审计、断网失败、无 Bean 直调。

## SHA-256

- `01-WP05.T06-http-audit.log`: `4e603708ca5cd81d5c563a9fabac201065984794fd176c05eb8815b77387ddc5`
- `A2aCrossProcessHttpTest.java`: `f761d9b9a5a4016b6cb5d2529a8929e640837c47a1d3f39b63cad7954666ea91`
- `A2aArchitectureTest.java`: `a1d29ffd1626067fb423a2ae4f0b04d5882279301a726d5f29e0648d0aec2d00`
