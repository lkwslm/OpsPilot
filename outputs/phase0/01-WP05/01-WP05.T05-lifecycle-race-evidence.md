# 01-WP05.T05 Task 生命周期竞争证据

状态：`PASS`

取消与完成在 PostgreSQL `FOR UPDATE` 行锁上真实竞争，恰好一个终态写入成功，另一个收到 `TASK_TERMINAL`。终态 revision 固定为 3、事件数固定为 3；之后再次尝试 WORKING、COMPLETED 或 CANCELED 均被拒绝，数据库状态和事件数不变。

同一测试还验证重复发送返回同一 Task、另一个 stream 在首事件后断开，以及 Server/Client/Store 实例重建后仍读取相同终态和 revision。结果为 1 个测试通过，0 failure，0 error，`BUILD SUCCESS`。

## SHA-256

- `A2aLifecycleRaceTest.java`: `8d9d3cafec03a291e6baf8706d0b49097619d2ffd60a459f3a58bc0a648c706f`
