# 01-WP09.T03 挂载隔离证据

状态：`PASS`

机器审查和五个运行容器的 `docker inspect` 均确认 `/datasets/input` 只有一个 `RW=false` 挂载。Evidence Agent 容器内实测：

- 写 `/datasets/input/forbidden-write` 被拒绝为 `Read-only file system`；
- 不存在 `/datasets/ground-truth`、`/ground-truth`、`/execution` 和 `/var/run/docker.sock`；
- 不存在另一个 Agent 的 `/run/secrets/code-agent-token`。

容器只挂载 E 盘 `deployment/agent-input`、自身 service secret；Supervisor 额外挂载只读 Agent Directory。
