# 01-WP09.T04 启动、健康与网络证据

状态：`PASS`

- `db-migrate` 是六个进程的 `service_completed_successfully` 硬依赖，运行输出 `MIGRATION_SET_VALIDATED version=4`。
- `retrieval-model-probe` 仅在 `phase0-gate` profile 中运行，没有任何进程依赖它；一次性运行输出 `RETRIEVAL_GATE_CONFIGURATION_COMPATIBLE`。
- A2A 协议设为 `0.9` 时进程以退出码 1 和 `A2A_PROTOCOL_INCOMPATIBLE` 退出；chat 模型 ID 不匹配时 gate 以退出码 1 和 `CHAT_MODEL_ID_INCOMPATIBLE` 退出。
- 临时 Card endpoint 不可达时，同一运行进程 liveness 为 UP、readiness 为 DOWN；endpoint 启动后下一轮探针恢复 readiness UP。JUnit 恢复测试 1 test、0 failure，Surefire SHA-256 `d897fdf03a1304b09ca33e042cb33a73f971ac23af8b6aa89d604f32b6064458`。
- Supervisor 审计显示只通过 Compose DNS 的 HTTP URL 访问 `evidence-agent:8081`、`code-agent:8082`、`knowledge-agent:8083`、`diagnosis-agent:8084`、`remediation-agent:8085`，全部 HTTP 200；专业端口未发布到宿主机。
