# 01-WP09.T02 进程身份隔离证据

状态：`PASS`

六个进程的 `AGENT_ID`、数据库角色、A2A URL 和 secret 文件引用全部唯一；五个专业 Agent 的 skill 与 `agent-directory.yaml` 一致。Token 文件位于 E 盘项目 `.tmp/secrets`，被 Git 忽略，容器只挂载自己的 secret，证据不记录 Token 内容。

从 Evidence Agent 容器用 Evidence Token/skill/role 调用 Code Agent 得到 HTTP `403`；相同身份调用 Evidence Agent 得到 HTTP `200` 与 `COMPLETED`，并通过只读 JSONL Source 生成 Observation/Evidence。403 分支在生成 taskId 前返回，不产生 Task。两端审计只记录 Compose 内网 caller IP、方法和路径，不记录 Token；Evidence claim 中的 Source 凭证值为 `[REDACTED]`。

`agent-directory.yaml` SHA-256：`3eff80cea1ee366e55090c7a3a2eadb7d9f96849091576c2a4891f946031c018`
