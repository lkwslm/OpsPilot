# 01-WP07.T03 单活动 Run 并发证据

状态：`PASS`

- 部分唯一索引严格使用 `status NOT IN ('COMPLETED', 'FAILED', 'CANCELLED')` 定义活动 Run。
- 两个线程使用独立 JDBC 连接与事务，在同一闩锁后并发插入同一 Incident 的活动 Run。
- 结果为一个事务提交、一个 PostgreSQL `23505`；适配器仅在约束名为 `uq_incident_one_active_run` 时映射为 `INCIDENT_ACTIVE_RUN_EXISTS`，并携带已提交的 `runId`。
- 最终数据库查询确认活动 Run 数量为 1。

`V4__enforce_one_active_run.sql` SHA-256：`17267938fc192a4ddd27cd78a7bf4f572a6b44ae7f7e2b3bbcceafa1c53830a0`
