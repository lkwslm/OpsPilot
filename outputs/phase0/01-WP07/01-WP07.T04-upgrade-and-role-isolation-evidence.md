# 01-WP07.T04 升级与角色隔离证据

状态：`PASS`

- 空库直接执行 V1–V4 成功，最终严格得到 10 张 Phase 0 表。
- 生产前一版本路径先执行 V1–V3，确认唯一索引不存在，再在同一数据库升级 V4；升级成功且索引存在。
- `SET ROLE evidence_agent_role` 后写 `opspilot.incident` 被 PostgreSQL 以 SQLSTATE `42501` 拒绝。
- 同一专业 Agent 角色读取 `opspilot_eval.ground_truth` 被 PostgreSQL 以 SQLSTATE `42501` 拒绝。
- Flyway schema history 分别证明前一版本为 V3、升级后为 V4。
- 全套真实容器结果：4 tests，0 failure，0 error；Surefire XML SHA-256 `b7cdbe066444968195cd0288041b1b7854a1da13b37cec7d4e4b324db5082615`。
