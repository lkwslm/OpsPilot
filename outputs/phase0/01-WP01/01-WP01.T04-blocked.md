# 01-WP01.T04 阻塞解除记录

## 状态

`RESOLVED`（2026-07-18）

用户已授权检查并复用本地镜像、拉取缺失镜像，以及选择并固定 AgentScope/A2A 坐标与模型身份。所有缺失事实源均已解析到不可变版本或 digest，并写入 `deployment/versions.lock.yaml`。

## 缺失的事实源

- AgentScope Java：已验证的 Maven 坐标、精确版本和 License。
- A2A Java：与官方 `v1.0.1` 对应的 SDK/生成对象坐标、精确版本和 License。
- PostgreSQL/pgvector：验收镜像仓库、精确 digest 和 pgvector 版本。
- Infinity：验收镜像仓库和精确 digest。
- Embedding：模型 ID、不可变 revision、来源和 License。
- Rerank：模型 ID、不可变 revision、来源和 License。

## 仓库扫描结论

设计文件 `docs/design/opspilot-system-design/13-runtime-topology-and-phase0-gates.md` 的相应字段仍为 `<verified>`、`<id>` 和 `<immutable revision>`。Release Manifest 文件是合同示例，包含重复数字 digest 和 `selected-*` 占位模型，不能提升为供应链事实。

## 解除结果

- 本地复用 `pgvector/pgvector`，锁定 PostgreSQL 16.14、pgvector 0.8.4 与 RepoDigest。
- 缺失的 Infinity 镜像从官方仓库拉取，并锁定 0.0.77、RepoDigest 与本地 image ID。
- AgentScope Java 固定为 `io.agentscope:agentscope-core:2.0.0`。
- A2A 协议固定为官方 `v1.0.1` commit，Java SDK 固定为 `1.1.0.Final`。
- Embedding/Rerank 模型固定到明确 Hugging Face model ID、commit revision 与 License。

后续质量和性能仍由对应 Spike 门禁验证；失败时不得自动更换模型、SDK 或镜像版本。
